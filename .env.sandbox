# Zoho CRM Sandbox Configuration
# Based on the API documentation, sandbox uses different endpoints and tokens

# Copy from .env.local but modify for sandbox
API_KEY=dev-key-only-for-testing
USE_LANGGRAPH=true

# OpenAI Configuration (same as production)
OPENAI_API_KEY=sk-proj-...
OPENAI_MODEL=gpt-5

# Database (can use same or separate test DB)
DATABASE_URL=postgresql://adminuser:W3llDB2025Pass@well-intake-db-0903.postgres.database.azure.com:5432/wellintake_sandbox?sslmode=require

# Zoho Sandbox Configuration
ZOHO_SANDBOX_MODE=true
ZOHO_DC=com  # Change to your data center (com, eu, in, au)
ZOHO_API_BASE_URL=https://sandbox.zohoapis.com/crm/v8

# IMPORTANT: You need SANDBOX-SPECIFIC tokens!
# These tokens must be generated specifically for your sandbox organization
ZOHO_SANDBOX_CLIENT_ID=your_sandbox_client_id_here
ZOHO_SANDBOX_CLIENT_SECRET=your_sandbox_client_secret_here
ZOHO_SANDBOX_REFRESH_TOKEN=your_sandbox_refresh_token_here

# Use the same OAuth service but it needs sandbox tokens
ZOHO_OAUTH_SERVICE_URL=https://well-zoho-oauth-v2.azurewebsites.net

# Default owner (same as production)
ZOHO_DEFAULT_OWNER_EMAIL=daniel.romitelli@emailthewell.com

# Azure services (can be same)
AZURE_STORAGE_CONNECTION_STRING=...
AZURE_CONTAINER_NAME=email-attachments-sandbox

# Disable certain features for testing
SEND_TO_TALENTWELL=false
PUBLISH_TO_VAULT=false

# Other services
FIRECRAWL_API_KEY=fc-...