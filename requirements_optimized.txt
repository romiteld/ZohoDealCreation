# The Well Recruiting - Email Intake API Dependencies (OPTIMIZED)
# Minimal langchain dependencies for faster installation and reduced conflicts

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0
flask==3.0.0
pysqlite3-binary  # Required for ChromaDB on Azure App Service

# Azure Services
azure-storage-blob==12.19.0

# PostgreSQL (Azure Cosmos DB for PostgreSQL) with pgvector
asyncpg==0.29.0
psycopg2-binary==2.9.9
pgvector==0.2.4

# HTTP and API
requests==2.31.0
tenacity==8.2.3
httpx==0.27.2

# Data Processing
pydantic==2.8.2
python-multipart==0.0.6

# Email Processing
email-validator==2.1.0
beautifulsoup4==4.12.2
lxml==4.9.3

# Environment and Configuration
python-dotenv==1.0.0
setuptools==69.0.2

# AI and ML - CrewAI with MINIMAL LangChain dependencies
# ========================================================
# CRITICAL: Only include what's actually used in the code
crewai==0.159.0
openai==1.68.2  # Required by litellm (CrewAI dependency)

# MINIMAL LangChain Dependencies (only what's needed):
# For optimized version (crewai_manager_optimized.py):
langchain-openai==0.0.5  # ONLY for ChatOpenAI class
langchain-core==0.1.0    # Required by langchain-openai

# REMOVED unnecessary packages:
# - langchain==0.1.0         # NOT needed for optimized version
# - langchain-community      # NOT used anywhere in the code

# If using non-optimized version (crewai_manager.py), also add:
# langchain==0.1.0  # Only for Tool class in non-optimized version

numpy==1.26.2  # Used by various ML packages

# Web Research
firecrawl-py==0.0.16

# Optional: CrewAI Tools (only if using ScrapeWebsiteTool)
# crewai-tools==0.159.0  # Uncomment if using CrewAI's built-in tools

# Development and Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Logging and Monitoring
structlog==23.2.0

# CORS support
python-jose[cryptography]==3.3.0